export enum StatusEnum {
  CLOSED = 'closed',
  ONGOING = 'ongoing',
  READY = 'ready',
  NEW = 'new'
}

export enum TaskPriority {
  NORMAL = 'normal',
  FAILING = 'failing',
  SHORT_NOTICE = 'shortNotice',
  CLOSABLE = 'closable'
}
